using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewClass : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        print("yeet them feet");
    }

    // Update is called once per frame
    private void Update()
    {
        print("and I oop");
    }
}
